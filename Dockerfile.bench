FROM golang:1.21-alpine
RUN apk add --no-cache git
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN git init && git add -A && \
    git -c user.name="bench" -c user.email="bench@local" commit -m "bench"
CMD ["go", "run", "./benchmark/cmd/"]
